apiVersion: 1

providers:
  - name: ServerGuard
    orgId: 1
    folder: ServerGuard
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
