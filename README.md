# tinyserver

Tiny server home-lab runtime project.

Current goal:
- build a small managed server platform, not a pile of scripts
- use read-only telemetry first
- keep actions behind explicit operator adapters
